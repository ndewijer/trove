# sbp-skills manifest (machine-specific)
.sbp/

# Claude Code local config (machine-specific)
.claude/

# Local Photos.sqlite dumps — private photo metadata, never commit
photosdb/

# Compiled binary from `go build ./...`
/trove

# Local secrets / per-machine env (see .envrc.example for the shape).
.envrc
